import type { Wallet } from './wallet';
import arweave from 'arweave';

export class ArFSDAO {
	constructor(private readonly wallet: Wallet, arweave: Arweave) {}

	createDrive(driveName: string) {

	}
}
